"use client";
import {useEffect,useState} from "react";import {useParams} from "next/navigation";import Link from "next/link";import {authFetch} from "../../../lib/auth-fetch";
export default function ActionDetail(){const{id}=useParams();const[a,setA]=useState(null),[attachments,setAttachments]=useState([]),[error,setError]=useState(""),[message,setMessage]=useState(""),[file,setFile]=useState(null),[revision,setRevision]=useState(""),[description,setDescription]=useState(""),[uploading,setUploading]=useState(false);
async function load(){setError("");try{const[r,fr]=await Promise.all([authFetch(`/api/actions/${id}`),authFetch(`/api/actions/${id}/attachments`)]);const[j,fj]=await Promise.all([r.json(),fr.json()]);if(!r.ok)throw new Error(j.error);if(!fr.ok)throw new Error(fj.error);setA(j.action);setAttachments(fj.attachments||[])}catch(e){setError(e.message)}}
useEffect(()=>{if(id)load()},[id]);async function update(patch){setError("");setMessage("");try{const r=await authFetch(`/api/actions/${id}`,{method:"PATCH",body:JSON.stringify(patch)});const j=await r.json();if(!r.ok)throw new Error(j.error);setMessage("Action updated.");load()}catch(e){setError(e.message)}}
async function upload(e){e.preventDefault();if(!file)return setError("Choose a file first.");setUploading(true);setError("");setMessage("");try{const fd=new FormData();fd.append("file",file);fd.append("revision",revision);fd.append("description",description);const r=await authFetch(`/api/actions/${id}/attachments`,{method:"POST",body:fd});const j=await r.json();if(!r.ok)throw new Error(j.error);setFile(null);setRevision("");setDescription("");const input=document.getElementById("action-file-input");if(input)input.value="";setMessage(`File uploaded to Box folder ${j.folder?.name||""}.`);load()}catch(e){setError(e.message)}finally{setUploading(false)}}
if(!a)return <main className="admin-page"><Link className="back" href="/actions">← Actions</Link>{error||"Loading…"}</main>;
return <main className="admin-page"><Link className="back" href="/actions">← Actions</Link><div className="detail-head"><div><h1>{a.action_number}</h1><p>{a.title}</p></div><span className={`status-chip ${a.status}`}>{a.status}</span></div>{error&&<div className="notice error">{error}</div>}{message&&<div className="notice success">{message}</div>}<div className="detail-grid"><section className="admin-card"><h2>Action Details</h2><dl><dt>Project</dt><dd>{a.project?.code} — {a.project?.name}</dd><dt>Owner</dt><dd>{a.assignee?.full_name||"Unassigned"}</dd><dt>Originator</dt><dd>{a.originator?.full_name||"—"}</dd><dt>Due Date</dt><dd>{a.current_due_date||"—"}</dd><dt>Priority</dt><dd>{a.priority}</dd><dt>Progress</dt><dd>{a.progress}%</dd></dl><p>{a.description||"No description."}</p></section><section className="admin-card"><h2>Update Status</h2><div className="status-buttons">{["open","assigned","in_progress","under_review","completed","closed","on_hold"].map(s=><button key={s} onClick={()=>update({status:s})}>{s.replaceAll("_"," ")}</button>)}</div><label>Progress %</label><input type="number" min="0" max="100" defaultValue={a.progress||0} onBlur={e=>update({progress:Number(e.target.value)})}/></section></div><div className="attachment-grid"><section className="admin-card"><h2>Attach File to Action</h2><p className="muted">The actual file is uploaded to Box. Supabase stores only its metadata and relationship to this action.</p><form onSubmit={upload}><label>File</label><input id="action-file-input" type="file" onChange={e=>setFile(e.target.files?.[0]||null)} required/><label>Revision</label><input value={revision} onChange={e=>setRevision(e.target.value)} placeholder="e.g. Rev A"/><label>Description</label><textarea value={description} onChange={e=>setDescription(e.target.value)} placeholder="Purpose / review note"/><button className="wide-primary" disabled={uploading}>{uploading?"Uploading to Box…":"Upload Attachment"}</button></form></section><section className="admin-card attachment-list-card"><h2>Action Files ({attachments.length})</h2><table><thead><tr><th>File</th><th>Revision</th><th>Size</th><th>Uploaded</th><th>Review</th></tr></thead><tbody>{attachments.map(x=><tr key={x.id}><td>{x.file_name}</td><td>{x.revision||"—"}</td><td>{x.file_size_bytes?`${(x.file_size_bytes/1024/1024).toFixed(2)} MB`:"—"}</td><td>{x.uploaded_at?new Date(x.uploaded_at).toLocaleString():"—"}</td><td><Link className="small-action" href={`/actions/${id}/files/${x.id}`}>Open / Comment</Link></td></tr>)}{!attachments.length&&<tr><td colSpan="5" className="empty">No attachments yet.</td></tr>}</tbody></table></section></div></main>}
